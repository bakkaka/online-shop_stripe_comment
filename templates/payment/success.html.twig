{% extends 'base.html.twig' %}

{% block title %}Paiement Réussi{% endblock %}

{% block body %}
<div class="container text-center mt-5">
    <div class="card shadow-lg p-4">
        <div class="card-body">
            <h1 class="text-success"><i class="fas fa-check-circle"></i> Paiement Réussi !</h1>
            <p class="lead mt-3">Merci pour votre achat. Votre commande a été validée avec succès.</p>
            <div class="mt-4">
                <a href="{{ path('app_home') }}" class="btn btn-primary">
                    <i class="fas fa-home"></i> Retour à l'accueil
                </a>
               {# <a href="{{ path('user_orders') }}" class="btn btn-outline-secondary">
                    <i class="fas fa-receipt"></i> Voir mes commandes
                </a>#}
            </div>
        </div>
    </div>
</div>
{% endblock %}
